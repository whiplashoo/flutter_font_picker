name: flutter_font_picker
description: A Flutter widget that lets the user select a Google Font from a custom dropdown.
version: 0.1.0
homepage: https://github.com/whiplashoo/flutter_font_picker

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^2.1.0
  shared_preferences: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:


